---
import { iconsUrl } from "../lib/constants";

interface Props {
  stepNumber: string; // Cambiamos el tipo a number
}

const { stepNumber } = Astro.props;

// Convertimos el número a string para acceder al objeto iconsUrl
const Step = `Step${stepNumber}`;
---

<div class="flex flex-row items-center gap-3 justify-center">
  {
    iconsUrl.Step[stepNumber].map((icon: string) => (
      <img src={icon} class="w-12 h-12" alt="" />
    ))
  }
</div>
